import React from "react";

interface propType {
  setTitle: (state :boolean) => void;
}

const InsertTitle: React.FC<propType> = ({ setTitle }) => {
  return <>
  
  </>;
};

export default InsertTitle;
